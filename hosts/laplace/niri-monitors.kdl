// Laplace monitor configuration
// AMD GPU — no nvidia env vars needed (override applied via environment block)

environment {
    XDG_SESSION_TYPE "wayland"
    NIXOS_OZONE_WL "1"
    ELECTRON_OZONE_PLATFORM_HINT "auto"
    DISPLAY ":0"
}
