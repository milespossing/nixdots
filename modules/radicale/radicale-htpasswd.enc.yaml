# This file should contain a single key: radicale-htpasswd
# The value should be an htpasswd-format string (bcrypt), e.g.:
#   radicale-htpasswd: "miles:$2y$10$..."
#
# Generate with: htpasswd -nbB miles <password>
# Then encrypt: sops -e -i radicale-htpasswd.enc.yaml
